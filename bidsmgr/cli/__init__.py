"""CLI entry points.

Reference: architecture.md §12.

Each CLI verb is one module. Orchestration is explicit code, not a
Pipeline framework (see architecture.md §12 architectural rule 3
— the v1.0 rewrite's ``Pipeline`` orchestrator is a do-not-port).

Modules: ``scan``, ``classify``, ``plan``, ``convert``,
``validate``.

Stub — not yet implemented.
"""
